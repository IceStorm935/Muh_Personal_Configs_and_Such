Pretty straight forward, download the install folder and run the script. GG EZ
Note: You may need to add execute permissions to the script. Just put in this command to do that:
chmod +x (name of script)
